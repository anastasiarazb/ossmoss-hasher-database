#include <stdio.h> 
#include <string.h>

int wcount(char *s)
{
        int i,flag;
        char a,d,f;
        while (i != EOF)
        

int main ()
{
        char s[];
        long long r;
        gets(srt);
        r = wcount(s);
        printf("%d", r);
        return 0;    
}