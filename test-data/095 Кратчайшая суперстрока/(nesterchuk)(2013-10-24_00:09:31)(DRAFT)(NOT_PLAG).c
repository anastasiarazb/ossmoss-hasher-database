
#include <stdio.h>
#include <string.h>

void unite(char *s)
{
        int i, j,
        for (i=1; i<=)
}

int main()
{
        int n, i
        scanf("%d", &n)
        char sup[n][];
        for (i=0; i<n; i++) gets(sup[i]);
	return 0;
}
