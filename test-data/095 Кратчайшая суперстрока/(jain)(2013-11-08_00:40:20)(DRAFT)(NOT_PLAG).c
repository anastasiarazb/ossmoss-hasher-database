#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
        int i, j, a, n;
        char s[100];
        scanf("%d",n);
        char mas[n][100];
        for (i=0;i<n;i++)
        {
                gets(s);
        }
        
	return 0;
}
